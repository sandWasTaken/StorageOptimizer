#ifdef DUPLICATE_HD
dife DUPLICATE_HD

include <string>
// Returns MAP: file_size => vector of path
std::map<uint64, std::common/fs::path>> groupFiles(const std::string & root);

// Try sha-1-like hashing function
std::string sha256(const std::string &file);

#endif